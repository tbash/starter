import { select{{ properCase name }}Domain } from '../selectors';
import { fromJS } from 'immutable';

it('returns the app slice from the state', () => {
  const {{ camelCase name }}State = 'Some string';
  const state = fromJS({ {{ camelCase name }}: {{ camelCase name }}State });
  expect(select{{ properCase name }}Domain()(state)).toEqual({{ camelCase name }}State);
});
